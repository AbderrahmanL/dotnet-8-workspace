version: '3.4'
services:
  dev-env:
    container_name: dev-env
    build:
      context: ./dev-env
    volumes:
      - "..:/home/vscode/workspace"
      - "~/.ssh:/home/vscode/.ssh"
    stdin_open: true # docker run -i
    tty: true # docker run -t